export const routes = Object.freeze({
    EMPTY:   { name: 'empty',   path: '/' },
    HOME:    { name: 'home',    path: '/home' },
    LOGIN:   { name: 'login',   path: '/login' },
    AREAS:   { name: 'areas',   path: '/areas' },
    VOLUMES: { name: 'volumes', path: '/volumes' },
    AREA:    { name: 'area',    path: '/areas/:area' },
    VOLUME:  { name: 'volume',  path: '/volumes/:volume' }
});
